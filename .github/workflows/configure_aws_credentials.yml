name: Provisioning AWS Infrastructure

on:
  workflow_call:
    inputs:
      aws_region:
        required: true
        type: string
    secrets:
      aws_access_key_id:
        required: true
      aws_secret_access_key:
        required: true
      aws_session_token:
        required: true
